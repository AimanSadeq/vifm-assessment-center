# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Local scratch (frame extraction, audio, temp work)
.tmp/

# Project export archives - never commit binary zips of the repo
*.zip

# PPTX manual generators (build tooling for docs/manuals/*.pptx — not app code)
.pptx-build/

# Claude Code local-only state — should never be committed.
# (.claude/settings.json is the shared, committed config.)
.claude/settings.local.json
.claude/launch-*.json
.claude/scheduled_tasks.lock
