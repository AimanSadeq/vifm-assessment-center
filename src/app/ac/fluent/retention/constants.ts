// Plain constants (not a "use server" module, so non-function exports are allowed).

/** Compliance: candidate data is retained for a maximum of 2 years. */
export const RETENTION_MONTHS = 24;
export const PURGE_CONFIRMATION = "PURGE FLUENT DATA";
